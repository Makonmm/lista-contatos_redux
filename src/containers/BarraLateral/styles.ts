import styled from 'styled-components'

export const Aside = styled.aside`
  padding: 16px;
  background-color: #1e272e;
  height: 100vh;
  border-shadow: 0 4px 4px rgba(0, 0, 0, 0.3);
  border-radius: 10px;
  width: 100%;
`

export const Filtros = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-gap: 8px;
  margin-top: 16px;
  border-radius: 10px;
`
